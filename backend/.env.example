# Postgres
DB_PASSWORD=secret
# JWT
JWT_SECRET=my-super-secret-jwt-key-that-is-at-least-256-bits-long
# SMTP (Mailtrap — optional, email verification works without it)
MAIL_HOST=sandbox.smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USER=api
MAIL_PASSWORD=
